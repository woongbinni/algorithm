#include <iostream>
using namespace std;
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int T, N;
int result;

int main(void){
    scanf("%d", &T);

    for(int tc=1; tc<=T; ++tc){
        scanf("%d", &N);

        for(int i=1; i<=N; ++i){

        }
        
        printf("#%d %d\n", tc, result);
    }
    return 0;
}